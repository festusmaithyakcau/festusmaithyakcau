name: Snake Game

on:
  push:
    branches:
      - main
      - update
  schedule:
    # Updates every 5 hours
    - cron: "0 */5 * * *"
  workflow_dispatch:

jobs:
  generate:
    permissions:
      contents: write
      actions: write
    runs-on: ubuntu-latest
    timeout-minutes: 5

    steps:
      # generates a snake game from a github user (<github_user_name>) contributions graph, output a svg animation at <svg_out_path>
      - name: generate github-contribution-grid-snake.svg
        uses: Platane/snk/svg-only@v3
        with:
          github_user_name: ${{ github.repository_owner }}
          outputs: |
            dist/github-contribution-grid-snake.svg
            dist/github-contribution-grid-snake-dark.svg?palette=github-dark

      # the live streak-stats.demolab.com endpoint is too slow (10-20s cold
      # starts) for GitHub's image proxy timeout, so pre-render it here and
      # serve the static file from the output branch instead
      - name: fetch streak-stats.svg
        run: |
          mkdir -p dist
          curl -sL --retry 3 --retry-delay 5 --max-time 40 \
            "https://streak-stats.demolab.com?user=${{ github.repository_owner }}&theme=chartreuse-dark" \
            -o dist/streak-stats.svg

      # push the content of <build_dir> to a branch
      # the content will be available at https://raw.githubusercontent.com/<github_user>/<repository>/<target_branch>/<file> , or as github page
      - name: push github-contribution-grid-snake.svg to the output branch
        uses: crazy-max/ghaction-github-pages@v3.1.0
        with:
          target_branch: output
          build_dir: dist
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}

      # only reached if the steps above succeeded — clears prior runs of this
      # workflow so history never accumulates beyond the run in progress
      - name: Delete previous workflow runs
        uses: Mattraks/delete-workflow-runs@v2
        with:
          token: ${{ secrets.GITHUB_TOKEN }}
          repository: ${{ github.repository }}
          workflow: snake.yml
          retain_days: 0
          keep_minimum_runs: 0